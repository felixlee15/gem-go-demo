type User {
  id: ID!
  name: String!
  email: String!
  tasks: [Task!]!
  completedTaskCount: Int!
}

extend type Query {
  users: [User!]!
}

extend type Mutation {
  createUser(name: String!, email: String!): User!
}
